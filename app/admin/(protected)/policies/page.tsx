import { PlaceholderPage } from "@/components/admin/PlaceholderPage";

export default function PoliciesPage() {
  return (
    <PlaceholderPage
      title="Policies"
      note="Policy editor UI is planned for the next build pass. Policy text is seeded and editable directly in the policies table until then."
    />
  );
}
