import { PlaceholderPage } from "@/components/admin/PlaceholderPage";

export default function FormsPage() {
  return (
    <PlaceholderPage
      title="Forms"
      note="Form builder UI is planned for the next build pass. The default pre-screening form is seeded and editable directly in the forms table until then."
    />
  );
}
