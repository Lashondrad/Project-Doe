import { PlaceholderPage } from "@/components/admin/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      note="Studio timezone, reminder templates, and business settings UI are planned for the next build pass. Timezone is currently hardcoded in app/api/availability/route.ts."
    />
  );
}
