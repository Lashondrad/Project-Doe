import { PlaceholderPage } from "@/components/admin/PlaceholderPage";

export default function CalendarPage() {
  return (
    <PlaceholderPage
      title="Calendar"
      note="Day/week/month calendar view is planned for the next build pass — appointment data already exists and is visible in Appointments and Dashboard."
    />
  );
}
