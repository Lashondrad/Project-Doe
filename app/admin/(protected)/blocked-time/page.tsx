import { PlaceholderPage } from "@/components/admin/PlaceholderPage";

export default function BlockedTimePage() {
  return (
    <PlaceholderPage
      title="Blocked Time"
      note="Blocked time management UI is planned for the next build pass. Rows can be managed directly in the blocked_time table via Supabase Studio until then."
    />
  );
}
