import { PlaceholderPage } from "@/components/admin/PlaceholderPage";

export default function AvailabilityPage() {
  return (
    <PlaceholderPage
      title="Availability"
      note="Business hours UI is planned for the next build pass. Rules can be managed directly in the availability_rules table via Supabase Studio until then."
    />
  );
}
